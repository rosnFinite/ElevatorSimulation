name: elevator
channels:
  - defaults
dependencies:
  - python=3.7
  - pytorch
  - matplotlib
  - plotly
  - pandas
  - pip
  - pip:
      - simpy
      - flask
prefix: /Users/fabio/opt/anaconda3/envs/elevator
