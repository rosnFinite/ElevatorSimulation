name: skyscraper
channels:
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - plotly
  - pandas
  - numpy
  - wandb
  - pip
  - dash
  - conda-forge::dash-mantine-components
  - pip:
      - simpy
      - dash-iconify
