name: elevator
channels:
  - defaults
dependencies:
  - python=3.7
  - pytorch
  - plotly
  - pandas
  - numpy
  - pip
  - dash
  - conda-forge::dash-mantine-components
  - pip:
      - simpy
      - dash-iconify